import express from "express"; import cors from "cors"; import { pool } from "./db.js"; import usuariosRouter from "./routes/usuarios.js"; import clientesRouter from "./routes/clientes.js"; import servicosRouter from "./routes/servicos.js"; import financeiroRouter from "./routes/financeiro.js"; const app = express(); app.use(cors()); app.use(express.json()); app.use("/api/usuarios", usuariosRouter); app.use("/api/clientes", clientesRouter); app.use("/api/servicos", servicosRouter); app.use("/api/financeiro", financeiroRouter); const PORT = process.env.PORT || 5000; app.listen(PORT, ()=>console.log("Backend rodando na porta "+PORT));